export class Task {
    constructor(id, title, status, dueDate, priority) {
        this.id = id;
        this.title = title;
        this.status = status;
        this.dueDate = dueDate;
        this.priority = priority;
    }
}